@page
@model PersonalDataModel
@{
    ViewData["Title"] = "اطلاعات شخصی";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-6">
        <p>حساب شما حاوی اطلاعات شخصی است که به ما داده اید. این صفحه به شما امکان می دهد آن داده ها را دانلود یا حذف کنید.</p>
        <p>
            <strong>با حذف این داده‌ها حساب شما برای همیشه حذف می‌شود و این اطلاعات قابل بازیابی نیست.</strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post">
            <button class="btn btn-primary my-2" type="submit">دانلود</button>
        </form>
        <p>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">حذف</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
